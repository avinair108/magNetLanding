# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI coding assistant instructions — this is a public repo, so agent
# guidance and internal team/process notes stay local-only, never committed.
CLAUDE.md
AGENTS.md

# Env files and local secrets — none exist in this repo today (it's a
# static site with no hardcoded keys), but this is public, so block them
# preemptively rather than waiting for one to almost get committed.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
